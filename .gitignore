# === Segreti e ambiente ===
.env

# === Sistema di progetto portabile: livello privato e ignorato (PROJECT-SYSTEM §6/10) ===
_notes/
CLAUDE.local.md
.claude/settings.local.json
_notes/.tmp-docx-*/
.tmp-docx-*/
# Documenti Word grezzi (binari). Dichiarare qui eventuali eccezioni curate, es:
#   !docs/specifica-ufficiale.docx
*.docx

# === Dati locali rigenerabili ===
# L'indice FTS5 si ricostruisce dal corpus; il corpus e' un clone locale di italia-corpus,
# tenuto fresco con git pull (non un submodule: vogliamo sempre l'ultima versione, non un
# commit fissato). Entrambi restano fuori da git. I codici fondamentali scaricati da
# Normattiva in data/codici-extra sono invece TRACCIATI (piccoli, stabili, spediti col repo).
data/index/
data/italia-corpus/
# Le classi di atti che il corpus principale non contiene (leggi ordinarie, decreti-legge
# vigenti, Costituzione), recuperate dall'API Open Data di Normattiva con
# scripts/fetch_normattiva.py: oltre diecimila atti, si ricostruiscono dalla fonte e non
# si spediscono nel repository. Vedi docs/audit-completezza-corpus.md.
data/normattiva-suppl/

# === Python (versiona il manifesto, ignora il derivato — §13) ===
.venv/
venv/
__pycache__/
*.py[cod]
*.egg-info/
.pytest_cache/

# === Node ===
node_modules/

# === OS / editor ===
.DS_Store
Thumbs.db
.idea/
.vscode/
